export const metadata = {
  title: "11. Governance",
  description:
    "Contribution models, the RFC process, semantic versioning, deprecation and migration strategy, breaking changes, ownership models and the release process.",
};

# 11. Governance

## Quick Glance

- Governance is the set of processes deciding who can change a design system, how a change gets reviewed, and how consumers find out — it's what lets a component API work as a real public API.
- Most large orgs converge on a hybrid contribution model: the design system team owns primitives (Button, Input, Dialog) directly; product teams propose composite patterns through an RFC.
- Semver for components is trickier than for a typical function: changing a default value or DOM structure can silently break consumers even when no TypeScript type changed.
- If asked "is a visual redesign breaking?": purely visual, token-driven changes ship as minor; changes big enough to break a consumer's CSS overrides or tests are major.
- Accessibility fixes (ARIA attributes, focus order, keyboard behavior) are a deliberate exception — ship as patch/minor even though behavior changed, because the old behavior was a bug, not a contract.
- Deprecations need both a runtime console warning and an IDE-visible `@deprecated` JSDoc tag, plus — past roughly 20-30 call sites — a codemod (commonly built with jscodeshift), not just a migration guide.
- The RFC process (popularized by Rust in 2014, adopted by React's `reactjs/rfcs` in 2017) should be scoped to new components and breaking changes only — applying it to trivial changes makes contributors route around it.
- Track adoption via telemetry or lint rules and gate breaking removals on measured adoption, never on a fixed calendar date alone.
- Real examples to cite: Shopify Polaris (public RFC process), React's `reactjs/rfcs`, Facebook's jscodeshift (built for large-scale codemods).
- Changesets (covered in Chapter 9) is the standard tool for automating version bumps and changelogs tied into the PR review process.

## Definition

**Governance**, in a design system context, is the set of processes that
decide who can change the system, how a proposed change gets reviewed
before it ships, how consumers find out about it, and how compatibility
is protected across version boundaries. It's not bureaucracy layered on
top of engineering work. It's what lets a component API act as a real
*public API* — one used, at the same time, by dozens or hundreds of
consuming teams who had no say in the decision to change it — without
every change turning into a coordination crisis.

A design system with excellent components and no governance eventually
degrades into the exact inconsistency it was built to prevent, just one
layer up. Instead of every product team inventing its own button, every
contributor to the design system starts inventing its own rules for
what's allowed to change and how.

## Problem Statement

A design system's component API gets imported, directly or indirectly,
by every team in the org building product UI. That's exactly what makes
it valuable — and exactly what makes changing it expensive in a way that
changing normal application code isn't.

If an app team changes an internal function's signature, they update
every call site in their own repo, run their own tests, and ship.
Simple. If a design system team changes `<Button>`'s prop signature,
every one of those consuming repos needs to update instead — repos the
design system team doesn't own, can't see the diff for, and often can't
even fully list. Each of those teams updates on its own release cycle,
on its own timeline, and sometimes only discovers the break when their
own CI fails after an upgrade.

This asymmetry — cheap to change, expensive for everyone else to absorb
— is the entire reason governance exists. Without a formal process, a
design system tends to fall into one of two failure modes at scale.

- **Too permissive**: any contributor can merge any change, so the
  component API drifts inconsistently, one PR at a time, until nobody —
  including the design system team — can say with confidence what's
  stable and what might change next release.
- **Too centralized and slow**: only a small core team can touch
  anything. Every product team's real, valid need for a new component or
  variant queues up behind that team's bandwidth. Product teams start
  building their own one-off components outside the system just to ship
  on time — which is exactly the fragmentation the design system was
  supposed to prevent in the first place.

Governance is the ongoing discipline of finding, and re-tuning, the
right point between those two failure modes for your org's size and risk
tolerance.

## Why Does This Exist?

Design systems didn't always need formal governance. Early component
libraries were often small enough, and used by few enough teams, that
informally "asking in Slack" worked fine.

Governance processes showed up once design systems crossed a scale
threshold: dozens of consuming teams, hundreds of component usages
across the codebase, multiple contributors with legitimate but
conflicting opinions about API shape. Past that point, the cost of an
uncoordinated breaking change — multiplied across every consumer —
started to outweigh the cost of a formal review step.

This is the same underlying economics behind any widely-depended-upon
public API. TC39's stage process for JavaScript language changes,
Kubernetes' KEP (Kubernetes Enhancement Proposal) process, and React's
own RFC process before major API additions all exist for the same
reason: the audience for the change is too large, and too disconnected
from the people making the change, for informal coordination to scale.

## Historical Context

Semantic Versioning (semver.org, formalized by Tom Preston-Werner in
2010) predates most modern design system governance practice, and
everything else in this chapter builds on it. It gave the ecosystem a
shared vocabulary — major.minor.patch — for communicating compatibility.
RFC processes and deprecation strategies both depend on that shared
vocabulary to mean anything.

The RFC-as-a-formal-process pattern was popularized by Rust (RFCs
starting in 2014) and widely adopted by React (the `reactjs/rfcs` repo,
started in 2017, used for Hooks, Concurrent Mode, and Server
Components). Both languages picked this pattern because their API
surface was used by huge, disconnected audiences who couldn't be
coordinated through informal discussion alone. Design systems adopted
the same pattern once they hit comparable scale — Shopify's Polaris,
Atlassian's Design System, and IBM's Carbon all run public or internal
RFC processes today. It's a directly borrowed pattern, not something
design systems reinvented on their own.

Codemods (small programs that automatically rewrite code for you) trace
back to Facebook's internal tooling for migrating its enormous
first-party React codebase. jscodeshift — still the dominant codemod
toolkit today — was built and open-sourced by Facebook because manually
migrating tens of thousands of call sites across their monorepo, every
time a core API changed, wasn't something humans could do reliably by
hand. React itself has shipped official codemods for major API
transitions (the `react-codemod` repo covers things like the
`createClass`-to-ES6-class migration and removing the legacy context
API). Design systems adopted the same practice once their own consumer
count made manual migration guides no longer good enough on their own.

Changesets — the tool most commonly used today to formalize the
version-bump-and-changelog step of a release (covered in depth in
[Chapter 9](/chapters/monorepo-architecture)) — grew out of the broader
JavaScript monorepo tooling wave of the late 2010s. It's now close to
the default choice for open-source and internal design systems alike,
because it ties semver discipline directly into the PR review process,
instead of leaving it up to a release manager's memory.

## How It Works Internally

Mechanically, governance is a pipeline with explicit gates, not one
single decision point. A proposed change — a new component, a new
variant, a breaking API change — moves through stages:

1. It enters at the RFC stage, gets reviewed, and is accepted, rejected,
   or sent back for revision.
2. Once accepted, it gets implemented.
3. It passes through visual and API review (Storybook/Chromatic,
   covered in [Chapter 7](/chapters/storybook)).
4. It gets versioned and released through an automated pipeline.
5. Only then does it reach consumers, who upgrade on their own schedule
   — sometimes with the help of a migration codemod.

Each gate exists to catch one specific kind of problem before it reaches
the next, more expensive stage. An API design flaw caught at the RFC
stage costs a conversation. The same flaw caught after a major version
has shipped to 40 consuming teams costs a second breaking release.

It's worth being precise about the semver mechanics that make consumers
trust this process at all, because "is this a breaking change?" is
genuinely harder to answer for a UI component than for a typical
function signature. Getting it wrong — shipping a break as a minor
version — is exactly how consumer trust in the version number erodes.

A component's public API is more than its TypeScript prop types. It's
everything a reasonable consumer could depend on: prop names and types,
yes, but also default values, whether a specific class name is present,
the DOM structure a consumer might have written a CSS selector or a
Playwright test against, and even certain visual behaviors if they're
documented as guaranteed (for example, "this tooltip always renders in a
portal").

Teams that treat semver as purely "did a TypeScript type change"
under-count breaking changes and burn consumer trust. Teams that treat
every visual pixel shift as breaking over-count, and end up never
shipping a minor release — which pushes real improvements into an
ever-delayed major version.

A workable rule set, used in some form by most mature design system
teams:

- **Removing or renaming a prop, narrowing a prop's type, or changing a
  prop's default value — major.** Default values are especially easy to
  miss. Changing `<Button size="md">`'s default from `md` to `lg`
  silently changes every consumer who never explicitly set `size` — and
  no type error will catch it.
- **Adding a new required prop — major.** Adding an *optional* prop with
  a sensible default is not breaking.
- **A visual redesign that changes size, spacing, or default color,
  outside of a design token change consumers already opted into.** This
  is the genuinely contentious case. Most mature teams treat it as
  **minor at most**: if the change is purely visual, and doesn't touch
  the API surface or documented behavior, it ships as a minor with a
  prominent changelog and visual-diff documentation — *not* a major.
  Gating every visual refinement behind a major version would make
  continuous design improvement practically impossible at any
  reasonable speed. The exception that does warrant a major: a redesign
  big enough to break a consumer's own custom overrides — for example,
  they wrote CSS assuming a specific padding value that just changed.
  This is why the DOM-structure rule below matters more than the
  purely-visual one.
- **A DOM structure change that breaks a consumer's CSS selector or test
  targeting internal markup — major**, treated this way by most
  disciplined teams *even though the public props API didn't change at
  all*. The moment consumers can reach into your DOM output, it becomes
  part of the effectively-public contract. The right long-term fix — and
  why most modern design systems push consumers toward it — is to never
  let internal DOM structure be a supported extension point in the
  first place. Force styling through documented `className`/`style`
  props or CSS custom properties (see
  [Chapter 6](/chapters/styling-architecture)), instead of letting "just
  add a CSS selector for `.my-button > span`" become a contract nobody
  actually agreed to.
- **Accessibility fixes that change ARIA attributes, focus order, or
  keyboard behavior — usually a patch or minor**, even though behavior
  technically changed. The old behavior was a bug, not a supported
  contract, and blocking accessibility fixes behind a major version
  actively hurts the org's compliance posture. This is a deliberate,
  documented exception to "any observable behavior change is breaking."
  Be ready to justify it explicitly in an interview: treating an
  accessibility bugfix as major just because "behavior changed"
  optimizes for the wrong thing.

<Callout type="warning" title="The hardest semver call: internal DOM structure">
  Whether a DOM-structure change is breaking is the single most
  interview-worthy semver nuance in this space. The honest answer is "it
  depends on what you told consumers was supported" — which is exactly
  why disciplined design systems draw a hard, explicit line in their
  documentation: only props, documented CSS custom properties, and
  documented data attributes are part of the contract. Internal markup
  is explicitly *not* covered by semver guarantees. That has to be
  written down in the docs, not just assumed — otherwise you'll still
  get bug reports treating an internal refactor as a broken promise.
</Callout>

## Architecture

Here's the realistic end-to-end pipeline — from a team wanting a new
component or a breaking API change, to that change safely reaching every
consumer's production app:

```mermaid
graph TD
  A[Idea: new component /<br/>breaking API change] --> B[RFC written & posted]
  B --> C{RFC review}
  C -->|Rejected| D[Closed with rationale]
  C -->|Needs revision| B
  C -->|Accepted| E[Implementation PR]
  E --> F[Storybook + visual review<br/>Chromatic diff]
  F --> G[Changeset added to PR]
  G --> H{CODEOWNERS<br/>review gate}
  H -->|Changes requested| E
  H -->|Approved| I[Merge to main]
  I --> J[CI: version bump +<br/>changelog generation]
  J --> K[npm publish]
  K --> L{Breaking change?}
  L -->|Yes| M[Deprecation window on old API<br/>+ codemod shipped]
  L -->|No| N[Consumer upgrades on own schedule]
  M --> N
  N --> O[Adoption dashboard tracks<br/>upgrade %]
```

Every arrow that loops back — RFC revision, changes requested — is a
deliberate, cheap exit ramp. The whole point of this pipeline is to make
disagreement expensive to *ignore* but cheap to *resolve* early. The
alternative is disagreement surfacing after publish, which is expensive
for everyone.

## Real-World Example

Shopify's Polaris runs a public RFC process, documented in their
open-source repo. Polaris is used both internally, by Shopify's own
admin surfaces, and externally, by third-party app developers building
for the Shopify App Store — two audiences with very different upgrade
speeds and very different tolerance for breaking changes. A change
that's trivial for Shopify's own internal teams to absorb (they can just
coordinate an internal migration sprint) can be a real burden for an
independent third-party developer maintaining one app on the side. An
RFC process with public comments surfaces exactly that kind of mismatch
before a change ships, not after.

React's own `reactjs/rfcs` process is worth studying even though React
isn't a "design system" in the product sense — the scale problem is
structurally identical. An API change affecting millions of downstream
repos can't be coordinated through direct conversation, so the RFC repo,
with its structured proposal, discussion, and decision stages, acts as a
substitute for that conversation. Concurrent Mode and Server Components
both went through multi-month RFC and experimental-release periods,
specifically to surface consumer-side problems (library compatibility,
SSR edge cases) before the API was locked into a stable release. That's
the same "expensive to reverse, so front-load the disagreement" logic
that motivates a design system's RFC gate on component APIs.

## Implementation Example

Here's an RFC template, adapted for a component-API-specific proposal.
This shouldn't stay theoretical — teams should check it into
`.github/RFC_TEMPLATE.md` (or equivalent) and require it for any new
component or breaking change:

```markdown
# RFC: <title>

**Status:** Draft | In Review | Accepted | Rejected | Superseded
**Author(s):**
**Created:**
**Target release:** (e.g. v5.0.0 — only if this is a breaking change)

## Problem

What consumer-facing problem does this solve? Cite at least one real
consuming team's actual need, not a hypothetical — an RFC motivated by
"this seems more consistent" without a named consumer pain point is a
strong signal the change isn't ready to propose yet.

## Proposed API

The exact prop signature / component API, as TypeScript, not prose:

\`\`\`tsx
interface ComboboxProps {
  value: string | string[];
  onValueChange: (value: string | string[]) => void;
  multiple?: boolean;
  // ...
}
\`\`\`

Include a realistic usage example, not just the type signature.

## Alternatives considered

At minimum two other API shapes considered and why they were rejected —
an RFC with only one option presented hasn't actually explored the
design space, and reviewers should push back on it.

## Is this a breaking change?

State explicitly, using the semver rules in
[Chapter 11](/chapters/governance): does this remove/rename a prop,
change a default, alter DOM structure a consumer might target, or is it
purely additive?

## Migration cost (if breaking)

- Estimated number of consuming call sites affected (grep the monorepo /
  known consumer repos where possible).
- Can this be codemodded automatically? If yes, link the codemod PR or
  describe its scope. If no, explain why (e.g. requires a human judgment
  call the codemod can't make, like choosing between two now-distinct
  variants).
- Proposed deprecation window length.

## Open questions

Anything genuinely unresolved — an RFC that claims to have no open
questions is usually under-scoped.
```

Here's a `@deprecated` JSDoc annotation paired with a runtime console
warning. These are the two mechanisms consumers actually see day to day
— together, they make sure both an IDE-time signal and a runtime signal
fire:

```tsx
// The JSDoc @deprecated tag surfaces as a strikethrough in every
// consumer's IDE autocomplete and a TS/ESLint warning at the call site —
// this is often the FIRST signal a consumer sees, well before they read
// a changelog.
interface ButtonProps {
  /**
   * @deprecated Use `variant="secondary"` instead. `outline` will be
   * removed in v6.0.0 (see RFC-042). Codemod available:
   * `npx @acme/ds-codemod v5-outline-to-secondary`.
   */
  outline?: boolean;
  variant?: "primary" | "secondary" | "tertiary";
}

export function Button({ outline, variant, ...props }: ButtonProps) {
  // The runtime warning catches consumers on plain JS, or anyone whose
  // editor doesn't surface JSDoc deprecation — and, critically, logs a
  // component name + stack-adjacent context so a consuming team can
  // actually find the call site the warning refers to, not just know
  // "something, somewhere" used the deprecated prop.
  if (process.env.NODE_ENV !== "production" && outline !== undefined) {
    console.warn(
      "[@acme/ds] Button: `outline` is deprecated and will be removed " +
        "in v6.0.0. Use `variant=\"secondary\"` instead. " +
        "See https://ds.acme.com/rfcs/042 for details.",
    );
  }

  const resolvedVariant = outline ? "secondary" : variant;
  return <button data-variant={resolvedVariant} {...props} />;
}
```

Here's a minimal jscodeshift codemod for the same migration. It's the
kind of tool that turns a "manually update 340 call sites across 12
repos" migration guide into a single command a consuming team can run
against their own repo:

```ts
// codemods/v5-outline-to-secondary.ts
// Run with: jscodeshift -t this-file.ts --extensions=tsx,ts src/
import type { API, FileInfo, Options } from "jscodeshift";

export default function transform(
  file: FileInfo,
  api: API,
  _options: Options,
) {
  const j = api.jscodeshift;
  const root = j(file.source);

  // Find every <Button outline /> or <Button outline={true} /> JSX
  // element and rewrite it to variant="secondary" — this is the exact
  // shape of change a human doing find-and-replace across dozens of
  // repos gets wrong on edge cases (outline={someVar}, outline={false}),
  // which is precisely why an AST-based transform, not a regex, is
  // worth investing in for a migration this widely spread.
  root
    .find(j.JSXOpeningElement, { name: { name: "Button" } })
    .forEach((path) => {
      const attrs = path.node.attributes ?? [];
      const outlineAttrIndex = attrs.findIndex(
        (attr) =>
          attr.type === "JSXAttribute" && attr.name.name === "outline",
      );
      if (outlineAttrIndex === -1) return;

      const outlineAttr = attrs[outlineAttrIndex];
      const isStaticTrue =
        outlineAttr.type === "JSXAttribute" &&
        (outlineAttr.value === null ||
          (outlineAttr.value?.type === "JSXExpressionContainer" &&
            outlineAttr.value.expression.type === "BooleanLiteral" &&
            outlineAttr.value.expression.value === true));

      if (!isStaticTrue) {
        // Dynamic outline={someVariable} — can't safely auto-migrate,
        // flag it for manual review instead of silently guessing.
        console.warn(
          `Skipped dynamic \`outline\` prop in ${file.path} — needs manual migration.`,
        );
        return;
      }

      attrs.splice(
        outlineAttrIndex,
        1,
        j.jsxAttribute(j.jsxIdentifier("variant"), j.stringLiteral("secondary")),
      );
    });

  return root.toSource();
}
```

## Advantages

Formal governance turns "did this break me?" from a question consumers
answer by upgrading and finding out, into a question they can answer
from the changelog *before* upgrading. That predictability is what lets
large orgs upgrade a widely-depended-upon dependency with confidence
instead of dread.

An RFC process front-loads disagreement to the cheapest possible point —
a document, before any code exists — instead of the most expensive
point: a shipped breaking change discovered by 40 teams at the same
time.

Codemods turn a multi-week, error-prone, manually-tracked migration
spread across every consuming team into a single command each team runs
on its own schedule. That's a real order-of-magnitude improvement in
migration cost, at the scale of hundreds of call sites across dozens of
repos.

Clear ownership — CODEOWNERS-style review gates — means every change has
an accountable reviewer who knows the API's history. That catches
inconsistency a rotating or ownerless review process would miss.

And a codified release process (RFC → Storybook review → Changesets →
automated changelog → publish) removes the release manager as a single
point of failure and a bottleneck. Releases can happen whenever a PR
merges, not whenever one person has time to hand-assemble a changelog.

## Disadvantages

Every gate in this pipeline is also friction — real friction, not
imaginary. An RFC process that takes two weeks to get review comments
back is two weeks a product team spends blocked on a component they need
this sprint. If that happens often enough, teams start building around
the design system instead of through it — the exact failure mode
governance is supposed to prevent, now caused by governance itself being
miscalibrated.

Strict semver discipline, treating DOM structure changes as major,
means genuinely small internal refactors get gated behind a
major-version release cadence. That can slow down legitimate code health
work.

Codemods are themselves an engineering investment. Writing, testing, and
maintaining a jscodeshift transform for one specific migration is real
work, and it has to be weighed against just writing a good migration
guide and letting consumers migrate by hand — which is sometimes
genuinely cheaper for a migration touching only a handful of call sites.

And ownership gates — a single team or small group required to approve
every change — can become the exact bottleneck a federated contribution
model was meant to avoid, if that team's bandwidth doesn't grow along
with the org.

## Alternatives

The first governance decision a design system team makes is the
contribution model: who is allowed to propose and implement changes to
the system itself.

| Dimension | Centralized | Federated / Open-Contribution | Hybrid |
| --- | --- | --- | --- |
| What problem it solves | Guarantees a single, consistent quality bar and API design coherence | Scales component coverage beyond one team's bandwidth by letting product teams contribute directly | Balances coherence on core primitives with coverage on product-specific patterns |
| Why it was created | Small design systems teams protecting a young, still-forming API surface | Large orgs where the DS team's bandwidth became the org-wide bottleneck | Learned from both failure modes — pure centralized bottlenecks, pure federated fragments |
| Performance (org velocity) | Slower — every component change queues behind one team | Faster on raw throughput, but with quality variance | Fast for composite/product patterns, deliberate for primitives |
| Developer experience (for contributors) | Lower — product engineers can't self-serve a new component | Higher — product teams can build what they need without waiting | Mixed — depends whether your need is a primitive or a composite pattern |
| Learning curve (to contribute) | Low — few people need to learn the DS codebase deeply | Higher — many contributors need DS-specific conventions, a11y standards, token discipline | Moderate — RFC process gates the harder cases |
| Community / ecosystem support | Concentrated expertise, but a single point of failure if key people leave | Distributed expertise, more resilient to individual turnover | Distributed for composites, concentrated for primitives |
| Maintenance burden | Lower per-component (fewer authors, more consistent patterns) | Higher — inconsistent original authorship makes long-term maintenance harder | Moderate — primitives stay clean, composites need more review discipline |
| Enterprise suitability | High for small-to-mid orgs or early-stage systems | High for very large orgs (100+ product teams) needing coverage fast | High — the choice most large, mature orgs converge on |
| When to choose it | Small design systems team, early-stage API still finding its shape | Very large org, DS team bandwidth is the clear bottleneck, strong internal review culture | Org has both a stable primitive layer and constantly evolving product-specific needs |
| When NOT to choose it | Org has grown past what one team can review in reasonable time | API is still young/unstable — too many cooks before the shape is settled | Adds process overhead not worth it for a very small design system |

In practice, **hybrid** is where most large design system organizations
land, once they scale past roughly a dozen consuming teams. It's the
model used, in some form, by Atlassian, Shopify, and most other
companies with public governance documentation. The core design system
team keeps direct ownership of primitives — Button, Input, Dialog —
where API consistency and accessibility matter most, and where the cost
of getting it wrong is highest, since every composite pattern in the org
gets built on top of them. Product teams, meanwhile, can propose
composite patterns — say, a specific data-table configuration, or a
checkout-flow stepper — through an RFC that a smaller number of design
system reviewers approve, rather than fully re-implementing it
themselves.

Pure centralized models are common at earlier stages, when the API
surface is still being figured out and having many simultaneous
contributors would fragment a design language that hasn't settled yet.
But they don't scale past the point where the design system team's
bandwidth becomes the org's bottleneck — which shows up concretely as
product teams routing around the design system with their own one-off
components, exactly the fragmentation the system exists to prevent.

Pure federated/open-contribution models work at companies with an
unusually strong internal engineering review culture and a lower
tolerance for process (some open-source-native companies run something
close to this). But without a strong review gate, they tend to
accumulate exactly the inconsistency the design system was funded to
eliminate — five slightly different button implementations, none of
them quite matching the others' edge-case handling.

## Tradeoffs

Every governance decision trades velocity against coherence, and the
right balance genuinely shifts with org size. A rule set copied wholesale
from a 500-engineer org onto a 20-engineer one will feel like
bureaucratic overkill. The reverse will feel like chaos.

Running an RFC for *every* change, including trivial ones, wastes
reviewer time on genuinely low-risk changes. Most mature governance
processes scope the RFC requirement to new components and breaking/API
changes specifically, with a lighter fast-track (a single reviewer
approval) for bugfixes, additive optional props, and purely visual
non-breaking tweaks.

Treating semver strictly — DOM structure changes are major — protects
consumers who've built brittle CSS-selector-based overrides, but at the
cost of blocking healthy internal refactors. The better long-term fix is
documentation and lint rules that discourage consumers from relying on
internal DOM in the first place, so the strict rule becomes less
necessary to invoke over time.

Codemods cost real engineering time to write and test, which only pays
off past a certain number of affected call sites. A rough rule of thumb
some teams use: a migration affecting fewer than roughly 20-30 call
sites across the org is often cheaper to document as a manual migration
guide than to automate. Anything bigger clearly justifies the codemod
investment.

## Performance Considerations

Governance overhead is measurable, the same way engineering performance
is. The metrics that matter: time-to-merge for a proposed change,
time-to-adoption after a release, and the ratio of "changes shipped" to
"changes proposed but abandoned due to process friction."

A design system team should track RFC cycle time as a first-class
metric, the same way they'd track build time. An RFC that regularly
takes a month to get initial review feedback is a leading sign that the
process itself is the bottleneck — not the changes being proposed.

Release cadence matters too. Teams running Changesets-driven automated
releases (see [Chapter 9](/chapters/monorepo-architecture)) typically
ship far more often — sometimes daily for patch/minor releases — than
teams doing manual, human-coordinated releases, because the release step
itself stops being the rate limiter.

## Scalability Considerations

The contribution and ownership model that worked at 5 consuming teams
doesn't survive unchanged at 50.

The concrete signal that it's time to move from centralized to hybrid: a
growing backlog of legitimate composite-component requests the design
system team can't get to. Measure this directly, rather than waiting for
it to become an obvious crisis.

CODEOWNERS-style review gates need to grow their reviewer pool as the
contribution surface grows, or the same bottleneck reappears one layer
down — now it's "the three people who can approve PRs" instead of "the
one team that builds everything."

Adoption tracking — what percentage of consumers are on the latest major
version, what percentage still call deprecated APIs — becomes essential,
not optional, past a certain consumer count. With a handful of
consumers, you can just ask each team directly whether they've migrated.
Past a few dozen, you need a lint rule or a dashboard that queries
actual import usage across the org's repos, because manual tracking
silently goes stale.

## Common Mistakes

<Callout type="danger" title="Shipping a breaking DOM structure change as a patch release">
  A team refactors a `Dropdown` component's internal markup — say,
  removing a wrapper `<div>` that seemed purely cosmetic — and ships it
  as a patch, since no prop types changed. Some fraction of consumers
  had CSS targeting `.dropdown > div > ul` (often written years earlier,
  by someone who's since left the team), or a Playwright test asserting
  on that structure. The patch release breaks their production styling
  or CI, with a changelog entry that gave them zero warning, because
  "patch" implied "safe to auto-upgrade."

  The fix isn't just "never change DOM structure." It's documenting,
  explicitly, which parts of the DOM are and aren't covered by the
  semver contract — and treating structural changes to the
  documented-as-stable parts as major, even when no prop type changed.
</Callout>

<Callout type="danger" title="No deprecation window — removing an API in the same release that deprecates it">
  A team adds a `@deprecated` tag and removes the prop in the very next
  major version — with no runtime warning window in between where
  consumers on the *previous* major could actually see the deprecation
  notice before being forced to migrate. Consumers who don't read every
  changelog line by line (most don't) upgrade straight into a breaking
  removal, with no prior warning they'd ever seen in their own build
  output.

  A deprecation needs to: ship, generate a visible warning, exist for a
  real window (commonly one full major version cycle, sometimes longer
  for widely-used APIs), and only then get removed. The warning period
  is the actual mechanism that lets consumers migrate on their own
  schedule, instead of being forced to migrate and read a changelog on
  the same afternoon.
</Callout>

<Callout type="danger" title="RFC process applied uniformly to every change, including trivial ones">
  Requiring a full RFC — problem statement, alternatives considered,
  migration cost — for a one-line bugfix or an additive optional prop
  buries genuinely urgent, low-risk changes under the same process
  weight as a new primitive component. Contributors learn to route
  around the process ("merge without an RFC, just this once") instead of
  respecting it, which erodes the process's authority for the cases that
  actually need it.

  The fix is scoping RFC requirements explicitly: new components and
  breaking changes require one; bugfixes, additive optional props, and
  non-breaking visual polish get a lighter single-reviewer fast track.
</Callout>

<Callout type="danger" title="No adoption telemetry — assuming a deprecation window is 'enough' without measuring">
  A team sets a deprecation window (say, six months) purely by calendar,
  ships the removal on schedule, and only discovers afterward that a
  third of consumers were still on the deprecated API — because nobody
  was actually tracking adoption during the window.

  A fixed calendar date with no adoption data behind it is a guess, not
  a plan. Mature teams gate the actual removal on measured adoption
  crossing a threshold, not purely on elapsed time, and use that data to
  proactively reach out to the specific teams still lagging — instead of
  discovering the gap only when the breaking release lands.
</Callout>

## Best Practices

- Scope the RFC requirement to new components and breaking/API changes;
  give bugfixes and additive changes a lighter single-reviewer fast
  track.
- Write the semver rules down explicitly, including the DOM-structure
  and default-value edge cases — don't leave "is this breaking" to
  case-by-case judgment calls that different reviewers will answer
  differently.
- Always pair a deprecation with a runtime warning, an IDE-visible
  `@deprecated` JSDoc tag, and — for anything above roughly 20-30 known
  call sites — a codemod, not just a migration guide.
- Track adoption telemetry (lint rule or usage-analysis dashboard) and
  gate breaking removals on measured adoption, not just elapsed
  calendar time.
- Use CODEOWNERS or an equivalent review gate on the primitive layer
  specifically, even under a hybrid/federated model for composites.
- Automate the release step (Changesets or equivalent) so cadence isn't
  bottlenecked on a human release manager's availability.
- Revisit the contribution model deliberately as the org grows — moving
  from centralized to hybrid is a normal, expected scaling step, not a
  failure of the original model.
- Publish the RFC template and past RFC decisions somewhere consumers
  and contributors can browse — a searchable decision history is itself
  a governance asset, it prevents re-litigating settled questions.

## Interview Questions

<InterviewQuestion q="Is a visual redesign of a component a breaking change? Walk me through how you'd decide.">
  **What the interviewer is testing:** A nuanced understanding of semver
  applied to UI, not a memorized rule.

  **Poor answer:** "Yes, any visual change is breaking." Or: "No, visual
  changes are never breaking since types don't change."

  **Good answer:** Tells apart purely visual, non-API changes (usually
  minor) from changes that alter dimensions or DOM structure a consumer
  might have built brittle CSS overrides or tests against — the second
  kind can functionally break consumers even without a type change.

  **Excellent senior answer:** Gives the concrete rule most mature teams
  use: purely visual, token-driven refinements ship as minor with
  changelog and visual-diff documentation; changes big enough to break
  plausible consumer overrides (spacing or structure a selector might
  target) are major. Explains *why* — gating every visual polish behind
  a major would make continuous design improvement impractical — which
  is the real reason for the distinction, not just "because that's the
  convention."

  **Common mistakes:** Treating this as a binary yes/no without
  explaining where the actual line is drawn. Not connecting it to why
  the DOM-structure case specifically is the harder, more contentious
  one.
</InterviewQuestion>

<InterviewQuestion q="Why would a design system invest in a jscodeshift codemod instead of just writing a good migration guide?">
  **What the interviewer is testing:** Cost-benefit reasoning about
  migration tooling investment, and real familiarity with codemods as a
  practice — not just knowing the name.

  **Poor answer:** "Codemods are more modern and automated."

  **Good answer:** At scale — hundreds of call sites across many
  consuming repos — manual migration doesn't scale reliably. A codemod
  does the mechanical rewriting automatically and consistently.

  **Excellent senior answer:** Gives the actual crossover point (a rough
  heuristic: tens of call sites, a manual guide is fine; hundreds across
  many repos, a codemod clearly pays off). Acknowledges codemods
  themselves cost engineering time and can't handle every case (dynamic
  values that need human judgment, like the `outline={someVar}` case).
  Notes that a well-scoped codemod that flags what it can't safely
  migrate is more valuable than one that silently guesses wrong.

  **Common mistakes:** Presenting codemods as strictly better than
  migration guides in every case, without acknowledging the investment
  cost or the cases codemods genuinely can't automate.
</InterviewQuestion>

<InterviewQuestion q="How do you decide between a centralized and a federated contribution model for a design system?">
  **What the interviewer is testing:** Organizational judgment connecting
  team structure to system design, not just naming both models.

  **Poor answer:** "Federated is better because it scales."

  **Good answer:** Centralized protects quality and consistency, but
  bottlenecks on the design system team's bandwidth. Federated scales
  coverage but risks inconsistent quality without strong review
  discipline.

  **Excellent senior answer:** Names the hybrid model most large orgs
  converge on: the design system team owns primitives directly, product
  teams propose composites via RFC. Gives the concrete signal that it's
  time to move off pure centralized — a growing backlog of legitimate
  requests the design system team can't get to, or product teams
  building one-off components outside the system to avoid the wait.

  **Common mistakes:** Presenting this as a permanent, one-time choice,
  rather than something that should evolve with org size. Not being able
  to name the specific failure mode of each pure model.
</InterviewQuestion>

<InterviewQuestion q="A consuming team says a design system upgrade broke their app, and the changelog said it was a minor release. How do you investigate and what would make you conclude the versioning was wrong?">
  **What the interviewer is testing:** Practical debugging of a
  governance-process failure, and understanding of what counts as
  breaking beyond typed prop signatures.

  **Poor answer:** "I'd check if it was actually their bug, not ours."

  **Good answer:** Checks whether the change altered a default value,
  DOM structure, or any other implicit contract the consumer might
  reasonably have relied on — even if no TypeScript type changed.

  **Excellent senior answer:** Walks through the specific classes of
  "invisible breaking change": default value changes (silently affect
  anyone who didn't pass the prop explicitly) and DOM structure changes
  (break CSS selectors or tests targeting internal markup). If either
  turns out to be the root cause, concludes the versioning genuinely was
  wrong and should be corrected — a fix release, updated semver policy
  documentation, possibly a follow-up major if the break is significant
  enough — rather than just apologizing without fixing the underlying
  process gap.

  **Common mistakes:** Only checking the typed prop API and stopping
  there — missing the default-value and DOM-structure classes of
  breaking change that don't show up in a type diff.
</InterviewQuestion>

<InterviewQuestion q="What belongs in an RFC for a new design system component, and why does 'alternatives considered' matter as much as the proposed API?">
  **What the interviewer is testing:** Whether the candidate has actually
  written or reviewed real RFCs, versus knowing the term abstractly.

  **Poor answer:** "A description of the component and its props."

  **Good answer:** Problem statement, proposed API, alternatives
  considered, and migration cost (if it's a breaking change).
  Alternatives matter because an RFC that only presents one option
  hasn't actually explored the design space, and can't show the chosen
  shape is the right one.

  **Excellent senior answer:** Adds that the problem statement should
  cite a real, named consuming team's actual need — not a hypothetical —
  as the strongest signal an RFC is ready for review. Migration cost
  should include a concrete estimate of affected call sites and whether
  a codemod is possible, not just a vague "consumers will need to
  update."

  **Common mistakes:** Treating the RFC as pure API documentation,
  without the problem-justification and alternatives sections that make
  it a genuine decision record and not just a spec.
</InterviewQuestion>

<InterviewQuestion q="How do accessibility fixes interact with your semver policy — should a change to focus order or ARIA attributes be a major release?">
  **What the interviewer is testing:** Whether the candidate can identify
  a deliberate, justified exception to strict semver rules, rather than
  applying "any behavior change is breaking" mechanically.

  **Poor answer:** "Yes, any behavior change should be major to be
  safe."

  **Good answer:** Most teams ship accessibility fixes as patch or
  minor, even though behavior technically changed — because the old
  behavior was a bug, not a supported contract.

  **Excellent senior answer:** Explains the real cost of the strict
  alternative: gating accessibility fixes behind a major release
  materially slows down compliance work and actively hurts the
  organization's accessibility posture. Frames this as a deliberate,
  documented exception a team should be able to justify explicitly — not
  an oversight in the semver policy.

  **Common mistakes:** Not recognizing this as a genuine, defensible
  exception, and instead insisting on mechanical consistency that would
  actually be harmful in practice.
</InterviewQuestion>

## Manager-Level Questions

<ManagerQuestion q="Your design system's RFC process is taking a month on average to get review feedback, and product teams are starting to build components outside the system to avoid the wait. How do you fix this?">
  **What the interviewer is testing:** Process-health diagnosis and
  willingness to change your own team's process when it's the
  bottleneck, not just defend it.

  **Poor answer:** "I'd remind teams the RFC process is required."

  **Good answer:** Diagnoses whether RFC review is being applied too
  broadly — trivial changes going through the same heavyweight process
  as major ones — and scopes a lighter fast-track for lower-risk
  changes.

  **Excellent senior answer:** Treats "teams building outside the
  system" as the real leading business risk — it's the exact
  fragmentation the design system exists to prevent. Proposes both a
  process fix (scoped RFC requirements, a defined SLA for review
  turnaround) and, if bandwidth is the root cause, a structural fix
  (moving toward a hybrid contribution model so the design system team
  isn't the sole reviewer for every kind of change).

  **Common mistakes:** Treating the symptom (slow RFCs) and the root
  business risk (teams routing around the system) as separate problems,
  instead of connecting them. Defaulting to "add more process" instead
  of diagnosing whether existing process is miscalibrated.
</ManagerQuestion>

<ManagerQuestion q="How do you decide when it's worth shipping a breaking change versus finding a non-breaking alternative that achieves 80% of the same goal?">
  **What the interviewer is testing:** Cost-aware decision-making about
  breaking changes as an organizational, not just technical, cost.

  **Poor answer:** "Breaking changes are fine as long as we bump the
  major version."

  **Good answer:** Weighs the migration cost (estimated affected call
  sites, whether a codemod is feasible) against the value of the change,
  and actively looks for an additive alternative — a new prop alongside
  the old one, deprecated later — before committing to a hard break.

  **Excellent senior answer:** Frames the real cost of a breaking change
  as engineering time spread across every consuming team, not
  concentrated in the design system team. Uses that framing to justify
  preferring dual-export or feature-flagged transition periods over an
  immediate hard break, whenever the value doesn't clearly justify the
  distributed cost — with a concrete example of choosing the
  non-breaking 80% solution because the full breaking change wasn't
  worth the org-wide migration cost.

  **Common mistakes:** Evaluating the tradeoff purely from the design
  system team's own engineering cost, ignoring that the larger cost
  falls on every consuming team.
</ManagerQuestion>

<ManagerQuestion q="A senior engineer on a product team pushes back hard on your governance process, calling it bureaucratic overhead that slows them down. How do you respond?">
  **What the interviewer is testing:** Ability to defend a process on its
  actual merits and data, and genuine openness to the criticism being
  partially right, rather than defensive dismissal.

  **Poor answer:** "The process exists for a reason, they need to follow
  it."

  **Good answer:** Listens for the specific friction point (which gate,
  which kind of change), and checks whether the process is actually
  miscalibrated for that case — for example, a trivial change forced
  through a heavyweight RFC. Either fixes the process, or explains the
  concrete cost the process prevents, backed by a real past incident.

  **Excellent senior answer:** Treats the pushback as a genuine signal
  worth investigating, rather than something to defend against by
  default — proposes measuring RFC cycle time and adoption metrics to
  see if the complaint reflects a real process problem. Separately, for
  cases where the process really is correctly calibrated, reframes the
  conversation from "bureaucracy" to the concrete organizational cost
  being avoided — a specific past breaking-change incident that hurt
  multiple teams — making the tradeoff visible instead of just asserting
  authority.

  **Common mistakes:** Either giving in entirely without evaluating
  whether the process is actually working, or dismissing the feedback
  without checking whether it has merit.
</ManagerQuestion>

## Summary

Governance exists because a design system's component API works as a
genuine public API, used by many disconnected teams. Changing it is
cheap for the design system team to propose, but expensive for every
consuming team to absorb — that asymmetry means informal coordination
stops scaling past a modest number of consumers.

Most large organizations converge on a hybrid contribution model. The
design system team keeps direct ownership of primitives, where
consistency and accessibility matter most. Product teams propose
composite patterns through an RFC review gate, instead of the core team
fully re-implementing everything itself.

The RFC process front-loads disagreement to the cheapest point — a
document, before code exists. It should be scoped to new components and
breaking changes specifically, not applied to every low-risk change, or
contributors will just route around it.

Semantic versioning for a component library is genuinely trickier than
for a typical function API. Default-value changes and DOM-structure
changes can break consumers silently, even when no TypeScript type
changes — which is why disciplined teams write the semver rules down
explicitly, instead of leaving "is this breaking" to inconsistent
case-by-case judgment.

Deprecations need a real warning window: both an IDE-visible
`@deprecated` tag and a runtime console warning, paired with a
jscodeshift codemod once the affected call-site count justifies the
investment. A rough tens-of-call-sites threshold separates "write a
migration guide" from "invest in a codemod."

Adoption should be measured, not assumed, and breaking removals should
be gated on real adoption telemetry rather than a fixed calendar date.
None of this is free — every gate is also friction — which is why the
process itself needs periodic recalibration as the org and the design
system both grow.

**Key takeaways**

- Governance exists because a design system API is used by disconnected
  teams who can't be coordinated informally past a certain scale — it's
  a substitute for direct communication at scale.
- Hybrid contribution (DS team owns primitives, product teams propose
  composites via RFC) is where most large orgs land after outgrowing
  pure centralized or pure federated models.
- Semver for components is harder than for typical APIs: default-value
  changes and DOM-structure changes can silently break consumers even
  when the typed prop signature didn't change.
- Accessibility fixes are a deliberate, justified exception to strict
  semver — shipped as patch/minor even when behavior changes, because
  the old behavior was a bug, not a contract.
- Deprecations need both a visible warning window (JSDoc + runtime
  console warning) and, past a rough tens-of-call-sites threshold, a
  codemod — manual migration guides don't scale past that point.
- Adoption should be measured via telemetry/lint rules and used to gate
  breaking removals, not assumed from elapsed calendar time alone.

The release process described in this chapter — Changesets, automated
changelogs, and CI/CD publishing — is covered in mechanical detail in
[Monorepo Architecture](/chapters/monorepo-architecture). The build
artifacts that process actually publishes are covered in
[Packaging](/chapters/packaging).
